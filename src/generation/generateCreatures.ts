import { Building, Creature } from "../types/types";

const generateCreatures = (building: Building): Creature[] => {
  // Stub function for future implementation
  return [];
};

export default generateCreatures;
